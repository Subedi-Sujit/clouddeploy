# Copy this file to terraform.tfvars and adjust values.
# DO NOT commit terraform.tfvars to git (it contains secrets).

aws_region   = "ca-central-1"
project_name = "clouddeploy"

# Better: set this via environment variable:
#   export TF_VAR_db_password="<strong-password>"
# db_password = "change-me"
